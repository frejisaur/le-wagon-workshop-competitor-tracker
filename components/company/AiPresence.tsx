import {Table, TableBody, TableCell, TableHead, TableHeader, TableRow} from '@carbon/react';
import type {CompanyResponse} from '@/lib/domain/dashboard';
import {provenance, valueText} from './company-utils';

export function AiPresence({ai, countries}: Pick<CompanyResponse, 'ai' | 'countries'>) {
  const geographic = countries.filter((country) => (country.mentions ?? 0) !== 0 || (country.visibility ?? 0) !== 0);
  return <section className="ai-presence" aria-labelledby="ai-presence-heading"><div className="company-module__heading"><div><h2 id="ai-presence-heading">AI presence</h2><p>Observed visibility and provider-reported mention coverage.</p></div></div><p className="ai-presence__summary">Visibility is {valueText(ai.visibility, 'percent')} against a benchmark of {valueText(ai.benchmark, 'percent')}.</p><table className="company-table" aria-label="AI presence summary"><thead><tr><th scope="col">Metric</th><th scope="col" className="company-table__numeric">Value</th><th scope="col">Provenance</th></tr></thead><tbody><tr><td>Visibility</td><td className="company-table__numeric">{valueText(ai.visibility, 'percent')}</td><td>{provenance(ai.visibility)}</td></tr><tr><td>Benchmark</td><td className="company-table__numeric">{valueText(ai.benchmark, 'percent')}</td><td>{provenance(ai.benchmark)}</td></tr></tbody></table><div className="company-module__heading"><div><h3>Meaningful geography</h3><p>Zero-value country rows are hidden.</p></div></div>{geographic.length === 0 ? <p className="company-module__unavailable">No meaningful country-level AI observations were returned.</p> : <table className="company-table" aria-label="AI presence by country"><thead><tr><th scope="col">Country</th><th scope="col" className="company-table__numeric">Mentions</th><th scope="col" className="company-table__numeric">Visibility</th></tr></thead><tbody>{geographic.map((country) => <tr key={country.country}><td>{country.country}</td><td className="company-table__numeric">{valueText(country.mentions)}</td><td className="company-table__numeric">{valueText(country.visibility, 'percent')}</td></tr>)}</tbody></table>}<div className="company-module__heading"><div><h3>Model coverage</h3><p>Observed sample by reporting model.</p></div></div>{ai.byLlm.length === 0 ? <p className="company-module__unavailable">No model-level AI observations were returned.</p> : <table className="company-table" aria-label="AI presence by model"><thead><tr><th scope="col">Model</th><th scope="col" className="company-table__numeric">Mentions</th><th scope="col" className="company-table__numeric">Self mentions</th><th scope="col" className="company-table__numeric">Cited pages</th></tr></thead><tbody>{ai.byLlm.map((row) => <tr key={row.llm}><td>{row.llm}</td><td className="company-table__numeric">{valueText(row.mentions)}</td><td className="company-table__numeric">{valueText(row.selfMentions)}</td><td className="company-table__numeric">{valueText(row.citedPages)}</td></tr>)}</tbody></table>}</section>;
}
