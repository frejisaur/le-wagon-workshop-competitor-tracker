# Prompt 02 — author the data skill

Outcome: create `workshop/generated/competitor-data-contracts/SKILL.md` from the rules you can justify in `workshop/starters/data-join-brief.md`.

Read only that starter, its named fixtures/test, and `.agents/skills/competitor-data-contracts/SKILL.md` after your first draft for convergence. Use the `pipeline-builder` boundary. `workshop/generated/` is non-discoverable and disposable. Do not call providers, mutate data, paste fixture rows, or propose replacing the canonical skill before the audit passes.

Run the audit command in the starter. Return only: candidate path, word count, missing audit rules, tests run, and whether the canonical skill should change. Do not paste the full skill into chat.
