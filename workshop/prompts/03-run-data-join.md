# Prompt 03 — run the data join

Outcome: validate the sanitized Apollo + Semrush join and its exception counts. Read `workshop/context/01-data-join-brief.md` and only its named paths. Delegate only to `pipeline-builder`; use repository commands as the boundary. Do not call providers, mutate Airtable, paste rows, or change dashboard code. Return only command status, accepted/rejected counts, rejection-code counts, tests, changed paths, and fallback used.
