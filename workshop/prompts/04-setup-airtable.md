# Prompt 04 — set up Airtable

Outcome: inspect and bootstrap the disposable workshop base after explicit operator approval. Read `workshop/context/02-airtable-brief.md` and `workshop/credentials.md`. Use Airtable MCP for inspection and repository CLI for schema/import. Do not reveal credentials, touch another base, make destructive changes, or paste records. Return only preflight, base label, schema status, counts, command status, and fallback used.
