# Prompt 06 — build the dashboard

Outcome: implement the selected All Companies reference with complete responsive and data-quality states. Read `workshop/context/03-dashboard-brief.md` and only its named references. Delegate only to `dashboard-builder`. Do not redesign Company Detail, alter API schemas, fetch providers, or build the whole app. Return only changed paths, states, responsive/accessibility checks, tests and status, blocked boundaries, and fallback used.
